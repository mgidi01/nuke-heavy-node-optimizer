# nuke-heavy-node-optimizer
Nuke Python tool to quickly toggle, disable, or enable heavy nodes (Kronos, Denoise, Deep, etc.) to speed up script interaction. Includes a small GUI to manage which node classes are treated as heavy and to save/share presets.
